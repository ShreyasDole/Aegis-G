# ============================================
# Aegis-G — Backend Dependencies
# ============================================

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=21.2.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.17
httpx>=0.28.0

# Database
sqlalchemy>=2.0.36
alembic>=1.12.0
psycopg2-binary>=2.9.10
pgvector>=0.2.4

# Graph Database
neo4j>=5.25.0

# Authentication (JWT + password hashing)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1      # Pinned: passlib 1.7.x is incompatible with bcrypt 5.x
email-validator>=2.2.0

# MCP (Model Context Protocol) - Google Cloud MCP / remote servers
# https://modelcontextprotocol.github.io/python-sdk/
# https://docs.cloud.google.com/mcp
mcp>=1.0.0
google-auth>=2.25.0

# AI — google-genai (unified SDK, replaces deprecated google-generativeai)
google-genai>=1.0.0

# Cache
redis>=5.2.0

# MCP — Model Context Protocol
mcp>=1.0.0

# STIX 2.1 — Cyber threat intelligence standard
stix2>=3.0.0

# Utilities
numpy>=1.26.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
