====================================================================
RAILWAY ENVIRONMENT VARIABLES
====================================================================
Copy this into Railway → Backend Service → Variables → RAW Editor
Replace XXXXX with actual values from each service
====================================================================

ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# ── Security (GENERATED - DO NOT CHANGE) ──────────────────────────
SECRET_KEY=89d2e1216bf13829db8a94dc5b21158751dc4d20e958fbd0c4cd2d80ca322a40
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ── AI ─────────────────────────────────────────────────────────────
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=REPLACE_WITH_YOUR_GEMINI_KEY
GEMINI_FLASH_MODEL=gemini-2.5-flash

# ── PostgreSQL ─────────────────────────────────────────────────────
# Auto-filled by Railway PostgreSQL plugin
# Copy from: Railway → PostgreSQL service → Variables → DATABASE_URL
DATABASE_URL=${{Postgres.DATABASE_URL}}

# ── Redis ──────────────────────────────────────────────────────────
# Auto-filled by Railway Redis plugin
# Copy from: Railway → Redis service → Variables
REDIS_HOST=${{Redis.REDIS_HOST}}
REDIS_PORT=${{Redis.REDIS_PORT}}

# ── Neo4j Aura ─────────────────────────────────────────────────────
# Get from: https://console.neo4j.io (after creating free instance)
# Format: neo4j+s://XXXXXXXX.databases.neo4j.io
NEO4J_URI=REPLACE_WITH_AURA_URI
NEO4J_USER=neo4j
NEO4J_PASSWORD=REPLACE_WITH_AURA_PASSWORD

# ── CORS (UPDATE AFTER VERCEL DEPLOYMENT) ─────────────────────────
# Format: https://your-app.vercel.app
CORS_ORIGINS=https://REPLACE_WITH_VERCEL_URL
FRONTEND_URL=https://REPLACE_WITH_VERCEL_URL

# ── Optional (leave empty unless using) ───────────────────────────
OUTLOOK_CLIENT_ID=
OUTLOOK_TENANT_ID=
OUTLOOK_CLIENT_SECRET=
ENCRYPTION_KEY=

====================================================================
QUICK REFERENCE
====================================================================

1. Deploy PostgreSQL + Redis on Railway
2. Copy DATABASE_URL and REDIS_* vars
3. Create Neo4j Aura instance → get URI + password
4. Get Gemini API key from Google AI Studio
5. Paste this into Railway Variables
6. Generate domain for backend
7. Deploy Vercel frontend with NEXT_PUBLIC_API_URL
8. Update CORS_ORIGINS and FRONTEND_URL with Vercel URL
9. Redeploy backend

====================================================================
