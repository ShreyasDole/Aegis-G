# ============================================
# 🧪 Aegis-G Test Dependencies
# ============================================
# Single install: pip install -r requirements-test.txt
# Strict pins below prevent anyio/pydantic/httpx upgrades that break TestClient.

-r requirements.txt

# --- Lock test client stack (FastAPI 0.104.1 + Starlette + httpx 0.27 accept app= in TestClient) ---
fastapi==0.104.1
starlette==0.27.0
httpx==0.27.0
anyio==3.7.1
pydantic==2.5.0
pydantic-settings==2.1.0

# --- App deps (so CI does not need a second pip install that upgrades the above) ---
google-genai>=1.62.0
stix2>=3.0.0

# Testing Framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0

# Mocking
pytest-mock>=3.12.0
responses>=0.24.0

# Code Quality
ruff>=0.1.0
black>=23.11.0
isort>=5.12.0
mypy>=1.7.0

# Security Scanning
bandit>=1.7.0
safety>=2.3.0

